# Dépendances ML lourdes — torch ecosystem.
# Rebuild: docker build -f backend/Dockerfile.base -t chatbot-local-ml-base .
# Temps estimé (cold) : 30–60 min

# Advanced document parsing (PDF layout AI, TableFormer)
docling>=2.0.0

# Reranker cross-encoder multilingue (partage torch avec docling)
FlagEmbedding>=1.3.0

# Reranker ColBERT late-interaction (activé via USE_COLBERT=true)
ragatouille>=0.0.9

# Embeddings locaux (sentence-transformers → torch + transformers)
sentence-transformers>=3.0.0
accelerate>=0.28.0
langchain-huggingface>=0.1.0

# RAG evaluation LLM-as-judge (ragas → datasets → pyarrow, tiktoken — heavy)
ragas>=0.2.0
