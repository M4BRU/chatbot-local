# Dépendances applicatives — pure Python, légères (~1–2 min cold).
# Ces packages changent le plus souvent → couche finale dans Dockerfile.base.

# FastAPI and server
fastapi>=0.128.0
uvicorn[standard]>=0.30.0
pydantic>=2.12.0
pydantic-settings>=2.4.0
python-multipart>=0.0.9

# SSE streaming
sse-starlette>=2.1.0

# HTTP clients
httpx>=0.27.0
requests>=2.32.0

# Document parsers (pure Python)
python-docx>=1.1.0

# API clients
anthropic>=0.45.0
openai>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
